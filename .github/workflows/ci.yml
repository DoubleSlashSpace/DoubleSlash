# Local mirror: scripts/ci_local.ps1 (Windows) / scripts/ci_local.sh (Linux/macOS)
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  RUST_TOOLCHAIN: "1.97.1"
  # Pinned so CI never builds "latest" cargo-audit, whose MSRV can outrun the
  # pinned toolchain (rustsec/audit-check did exactly that). Known-good on 1.97.
  CARGO_AUDIT_VERSION: "0.22.1"

jobs:
  test:
    name: Rust tests
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          submodules: recursive

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable (2025-04)
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          components: rustfmt, clippy

      - name: Cache Cargo registry + target
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust/target
            rust/doubleslash-client/target
            rust/doubleslash-supernode-manager/target
          key: linux-cargo-ci-${{ env.RUST_TOOLCHAIN }}-${{ hashFiles('rust/Cargo.lock') }}-${{ hashFiles('rust/doubleslash-client/Cargo.lock') }}-${{ hashFiles('rust/doubleslash-supernode-manager/Cargo.lock') }}

      - name: Install system dependencies (Linux)
        run: sudo apt-get update -qq && sudo apt-get install -y libasound2-dev libdbus-1-dev libclang-dev

      - name: Verify version metadata stays in sync
        run: pwsh scripts/check_version_sync.ps1

      - name: Cache Opus DNN model weights
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            rust/doubleslash-opus/opus/dnn/lace_data.c
            rust/doubleslash-opus/opus/dnn/lace_data.h
            rust/doubleslash-opus/opus/dnn/nolace_data.c
            rust/doubleslash-opus/opus/dnn/nolace_data.h
            rust/doubleslash-opus/opus/dnn/fargan_data.c
            rust/doubleslash-opus/opus/dnn/fargan_data.h
            rust/doubleslash-opus/opus/dnn/pitchdnn_data.c
            rust/doubleslash-opus/opus/dnn/pitchdnn_data.h
            rust/doubleslash-opus/opus/dnn/plc_data.c
            rust/doubleslash-opus/opus/dnn/plc_data.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_constants.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_enc_data.c
            rust/doubleslash-opus/opus/dnn/dred_rdovae_enc_data.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_dec_data.c
            rust/doubleslash-opus/opus/dnn/dred_rdovae_dec_data.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_stats_data.c
            rust/doubleslash-opus/opus/dnn/dred_rdovae_stats_data.h
            rust/doubleslash-opus/opus/dnn/bbwenet_data.c
            rust/doubleslash-opus/opus/dnn/bbwenet_data.h
            rust/doubleslash-opus/opus/dnn/lossgen_data.c
            rust/doubleslash-opus/opus/dnn/lossgen_data.h
            rust/doubleslash-opus/opus/dnn/models
          key: opus-dnn-tarlist-a5177ec6fb7d15058e99e57029746100121f68e4890b1467d4094aa336b6013e

      - name: Fetch Opus DNN model weights
        run: bash scripts/fetch_opus_weights.sh

      # --- Lint (fail fast) -------------------------------------------------
      - name: Check formatting (rust/ workspace)
        run: cargo fmt --all -- --check
        working-directory: rust

      # --all-targets so test code is linted too; without it a lint in a
      # #[cfg(test)] module only shows up on a developer's machine.
      - name: Clippy (rust/ workspace, deny warnings)
        run: cargo clippy --all --all-targets -- -D warnings
        working-directory: rust

      - name: Release manifest signer self-test
        run: cargo run -p doubleslash-installer --bin sign-release-manifest -- --self-test
        working-directory: rust

      # --- Tests ------------------------------------------------------------
      - name: Run Rust tests (rust/ workspace)
        working-directory: rust
        run: cargo test --all --release

      - name: Test client workspace (headless, no qt-ui feature)
        working-directory: rust/doubleslash-client
        run: cargo test

      # Client workspace also needs its own fmt/clippy pass (separate Cargo workspace)
      - name: Check formatting (client workspace)
        run: cargo fmt --all -- --check
        working-directory: rust/doubleslash-client

      - name: Clippy (client workspace, headless)
        run: cargo clippy -p doubleslash-client --no-default-features --all-targets -- -D warnings
        working-directory: rust/doubleslash-client

      # Lints the macOS-only capture module from Linux. Clippy type-checks
      # without linking, so the Rust half needs no Mac; this catches lints
      # that fire solely under cfg(target_os = "macos") without waiting on
      # the macOS runner. Only the Objective-C half genuinely needs macOS.
      - name: Clippy (macOS capture module, cross-linted)
        run: cargo clippy -p doubleslash-client --no-default-features --features lint-macos -- -D warnings
        working-directory: rust/doubleslash-client

      # The supernode manager is a third Cargo workspace; nothing above reaches it.
      - name: Check formatting (supernode-manager workspace)
        run: cargo fmt --all -- --check
        working-directory: rust/doubleslash-supernode-manager

      - name: Clippy (supernode-manager workspace, deny warnings)
        run: cargo clippy --all-targets -- -D warnings
        working-directory: rust/doubleslash-supernode-manager

      - name: Test supernode-manager workspace
        run: cargo test
        working-directory: rust/doubleslash-supernode-manager

      # --- Security ---------------------------------------------------------
      # Lockfiles live under rust/ (three separate Cargo workspaces, not repo root).
      # The RUSTSEC-2024-0436 ignore (macOS-only wgpu-hal→metal `paste`) lives
      # in rust/.cargo/audit.toml and is picked up automatically.
      - name: Cache cargo-audit binary
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.cargo/bin/cargo-audit
          key: linux-cargo-audit-${{ env.CARGO_AUDIT_VERSION }}

      - name: Install cargo-audit
        # Skip the build when the cached binary already reports the pinned
        # version (cargo install would rebuild — ~/.cargo/.crates.toml is not
        # part of the cache).
        run: |
          cargo-audit --version 2>/dev/null | grep -qF "$CARGO_AUDIT_VERSION" \
            || cargo install cargo-audit --version "$CARGO_AUDIT_VERSION" --locked

      - name: Security audit (rust workspace)
        run: cargo audit --file Cargo.lock
        working-directory: rust

      - name: Security audit (client workspace)
        run: cargo audit --file Cargo.lock
        working-directory: rust/doubleslash-client

      - name: Security audit (supernode-manager workspace)
        run: cargo audit --file Cargo.lock
        working-directory: rust/doubleslash-supernode-manager

  # Line/region coverage % via cargo-llvm-cov (report-only; raise floors later).
  # High-ROI packages: features + supernode + headless client. See scripts/coverage.sh.
  coverage:
    name: Rust coverage %
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          submodules: recursive

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable (2025-04)
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          components: llvm-tools-preview

      - name: Cache Cargo registry + target
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cargo/bin
            rust/target
            rust/doubleslash-client/target
          key: linux-cargo-cov-${{ env.RUST_TOOLCHAIN }}-${{ hashFiles('rust/Cargo.lock') }}-${{ hashFiles('rust/doubleslash-client/Cargo.lock') }}

      - name: Install system dependencies (Linux)
        run: sudo apt-get update -qq && sudo apt-get install -y libasound2-dev libdbus-1-dev libclang-dev

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@c7eb1735f09259a5035e8e5d44b1406b1cddc0fb # v2.83.0
        with:
          tool: cargo-llvm-cov@0.8.7

      - name: Cache Opus DNN model weights
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            rust/doubleslash-opus/opus/dnn/lace_data.c
            rust/doubleslash-opus/opus/dnn/lace_data.h
            rust/doubleslash-opus/opus/dnn/nolace_data.c
            rust/doubleslash-opus/opus/dnn/nolace_data.h
            rust/doubleslash-opus/opus/dnn/fargan_data.c
            rust/doubleslash-opus/opus/dnn/fargan_data.h
            rust/doubleslash-opus/opus/dnn/pitchdnn_data.c
            rust/doubleslash-opus/opus/dnn/pitchdnn_data.h
            rust/doubleslash-opus/opus/dnn/plc_data.c
            rust/doubleslash-opus/opus/dnn/plc_data.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_constants.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_enc_data.c
            rust/doubleslash-opus/opus/dnn/dred_rdovae_enc_data.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_dec_data.c
            rust/doubleslash-opus/opus/dnn/dred_rdovae_dec_data.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_stats_data.c
            rust/doubleslash-opus/opus/dnn/dred_rdovae_stats_data.h
            rust/doubleslash-opus/opus/dnn/bbwenet_data.c
            rust/doubleslash-opus/opus/dnn/bbwenet_data.h
            rust/doubleslash-opus/opus/dnn/lossgen_data.c
            rust/doubleslash-opus/opus/dnn/lossgen_data.h
            rust/doubleslash-opus/opus/dnn/models
          key: opus-dnn-tarlist-a5177ec6fb7d15058e99e57029746100121f68e4890b1467d4094aa336b6013e

      - name: Fetch Opus DNN model weights
        run: bash scripts/fetch_opus_weights.sh

      - name: Run coverage (hot packages)
        run: bash scripts/coverage.sh --scope hot --skip-install

      - name: Publish summary
        if: always()
        run: |
          if [ -f coverage/summary.md ]; then
            cat coverage/summary.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No coverage/summary.md produced" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload LCOV + JSON artifacts
        if: always()
        # v7 runs on Node 24 (v4 targets Node 20, which GH Actions now forces
        # onto 24 and annotates as deprecated). Keep the pin aligned with release.yml.
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: rust-coverage
          path: |
            coverage/
          if-no-files-found: warn
          retention-days: 14

  test-windows:
    name: Rust tests (Windows, non-Qt)
    runs-on: windows-latest
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          submodules: recursive

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable (2025-04)
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          components: rustfmt, clippy

      - name: Cache Cargo registry + target
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust/target
            rust/doubleslash-client/target
          key: windows-cargo-ci-ninja-${{ env.RUST_TOOLCHAIN }}-${{ hashFiles('rust/Cargo.lock') }}-${{ hashFiles('rust/doubleslash-client/Cargo.lock') }}

      - name: Verify version metadata stays in sync
        shell: pwsh
        run: pwsh scripts/check_version_sync.ps1

      - name: Cache Opus DNN model weights
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            rust/doubleslash-opus/opus/dnn/lace_data.c
            rust/doubleslash-opus/opus/dnn/lace_data.h
            rust/doubleslash-opus/opus/dnn/nolace_data.c
            rust/doubleslash-opus/opus/dnn/nolace_data.h
            rust/doubleslash-opus/opus/dnn/fargan_data.c
            rust/doubleslash-opus/opus/dnn/fargan_data.h
            rust/doubleslash-opus/opus/dnn/pitchdnn_data.c
            rust/doubleslash-opus/opus/dnn/pitchdnn_data.h
            rust/doubleslash-opus/opus/dnn/plc_data.c
            rust/doubleslash-opus/opus/dnn/plc_data.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_constants.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_enc_data.c
            rust/doubleslash-opus/opus/dnn/dred_rdovae_enc_data.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_dec_data.c
            rust/doubleslash-opus/opus/dnn/dred_rdovae_dec_data.h
            rust/doubleslash-opus/opus/dnn/dred_rdovae_stats_data.c
            rust/doubleslash-opus/opus/dnn/dred_rdovae_stats_data.h
            rust/doubleslash-opus/opus/dnn/bbwenet_data.c
            rust/doubleslash-opus/opus/dnn/bbwenet_data.h
            rust/doubleslash-opus/opus/dnn/lossgen_data.c
            rust/doubleslash-opus/opus/dnn/lossgen_data.h
            rust/doubleslash-opus/opus/dnn/models
          key: opus-dnn-tarlist-a5177ec6fb7d15058e99e57029746100121f68e4890b1467d4094aa336b6013e

      - name: Fetch Opus DNN model weights
        shell: pwsh
        run: pwsh scripts/fetch_opus_weights.ps1

      - name: Check formatting (rust/ workspace)
        shell: pwsh
        run: cargo fmt --all -- --check
        working-directory: rust

      - name: Clippy (rust/ workspace, deny warnings)
        shell: pwsh
        run: cargo clippy --all --all-targets -- -D warnings
        working-directory: rust

      - name: Release manifest signer self-test
        shell: pwsh
        run: cargo run -p doubleslash-installer --bin sign-release-manifest -- --self-test
        working-directory: rust

      - name: Run Rust tests (rust/ workspace, non-Qt)
        shell: pwsh
        run: cargo test --all --release
        working-directory: rust

      - name: Test client workspace (headless, no qt-ui)
        shell: pwsh
        run: cargo test
        working-directory: rust/doubleslash-client

      - name: Clippy (client workspace, headless)
        shell: pwsh
        run: cargo clippy -p doubleslash-client --no-default-features --all-targets -- -D warnings
        working-directory: rust/doubleslash-client

  test-linux-arm64:
    name: Supernode build (Linux ARM64)
    runs-on: ubuntu-24.04-arm
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          submodules: recursive

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable (2025-04)
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          components: rustfmt, clippy

      - name: Cache Cargo registry + target
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust/target
          key: linux-arm64-cargo-ci-${{ env.RUST_TOOLCHAIN }}-${{ hashFiles('rust/Cargo.lock') }}

      - name: Check formatting (rust/ workspace)
        run: cargo fmt --all -- --check
        working-directory: rust

      - name: Clippy (doubleslash-supernode, deny warnings)
        run: cargo clippy -p doubleslash-supernode -- -D warnings
        working-directory: rust

      - name: Run doubleslash-supernode tests
        run: cargo test -p doubleslash-supernode --release
        working-directory: rust

      - name: Install license tooling
        run: cargo install cargo-about --version 0.8.4 --locked

      - name: Package supernode (linux-aarch64)
        run: DOUBLESLASH_RELEASE=1 bash scripts/build_supernode.sh

  test-supernode-linux-x86_64:
    name: Supernode build (Linux x86_64)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          submodules: recursive

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable (2025-04)
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          components: rustfmt, clippy

      - name: Cache Cargo registry + target
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust/target
          key: linux-x86_64-supernode-ci-${{ env.RUST_TOOLCHAIN }}-${{ hashFiles('rust/Cargo.lock') }}

      - name: Clippy (doubleslash-supernode, deny warnings)
        run: cargo clippy -p doubleslash-supernode -- -D warnings
        working-directory: rust

      - name: Run doubleslash-supernode tests
        run: cargo test -p doubleslash-supernode --release
        working-directory: rust

      - name: Install license tooling
        run: cargo install cargo-about --version 0.8.4 --locked

      - name: Package supernode (linux-x86_64)
        run: DOUBLESLASH_RELEASE=1 bash scripts/build_supernode.sh

  test-supernode-windows:
    name: Supernode build (Windows)
    runs-on: windows-latest
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          submodules: recursive

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable (2025-04)
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          components: rustfmt, clippy

      - name: Cache Cargo registry + target
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust/target
          key: windows-supernode-ci-${{ env.RUST_TOOLCHAIN }}-${{ hashFiles('rust/Cargo.lock') }}

      - name: Clippy (doubleslash-supernode, deny warnings)
        shell: pwsh
        run: cargo clippy -p doubleslash-supernode -- -D warnings
        working-directory: rust

      - name: Run doubleslash-supernode tests
        shell: pwsh
        run: cargo test -p doubleslash-supernode --release
        working-directory: rust

      - name: Install license tooling
        shell: pwsh
        run: cargo install cargo-about --version 0.8.4 --locked

      - name: Package supernode (win64)
        shell: pwsh
        env:
          DOUBLESLASH_RELEASE: "1"
        run: pwsh scripts/build_supernode.ps1


  # macOS is the one platform with no local development machine on the team, so
  # this job exists to keep the AVFoundation capture path and the vendored VP8
  # build from bit-rotting unnoticed. It compiles and runs the headless tests;
  # it cannot exercise a camera (no hardware, and Screen Recording / Camera TCC
  # consent cannot be granted to a CI runner).
  test-macos:
    name: Rust tests (macOS, non-Qt)
    runs-on: macos-14
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          submodules: recursive

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable (2025-04)
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          components: rustfmt, clippy

      - name: Cache Cargo registry + target
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust/target
            rust/doubleslash-client/target
          key: macos-cargo-ci-${{ env.RUST_TOOLCHAIN }}-${{ hashFiles('rust/Cargo.lock') }}-${{ hashFiles('rust/doubleslash-client/Cargo.lock') }}

      - name: Cache Opus DNN model weights
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: rust/doubleslash-opus/opus/dnn
          key: opus-dnn-${{ hashFiles('scripts/fetch_opus_weights.sh') }}

      - name: Fetch Opus DNN model weights
        run: bash scripts/fetch_opus_weights.sh

      # The VP8 build shells out to libvpx's rtcd.pl; macOS ships perl in base,
      # but assert it rather than discovering its absence as a C compile error.
      - name: Verify perl is available (vendored libvpx codegen)
        run: perl --version

      - name: Build vendored VP8 (doubleslash-vpx)
        run: cargo test -p doubleslash-vpx
        working-directory: rust

      - name: Test client workspace (headless, no qt-ui)
        run: cargo test
        working-directory: rust/doubleslash-client

      - name: Clippy (client workspace, headless)
        run: cargo clippy -p doubleslash-client --no-default-features --all-targets -- -D warnings
        working-directory: rust/doubleslash-client
